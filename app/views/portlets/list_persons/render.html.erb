<% if @persons %>
<div class="persons">
	<% @persons.in_groups_of(3) do |group| %>
		<%= content_tag(:div, (group.collect! do |@person|
			<%= render :partial => 'person'  %>
		end ) ) %>
		<% end %>
	</div>
	<% end %>
